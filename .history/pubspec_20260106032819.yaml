name: todo_list_app

environment:
  sdk: ^3.9.0

dev_dependencies:
  melos: ^7.0.0

workspace:
  - packages/frontend
  - packages/backend
  - packages/shared

melos:
  scripts:
    analyze:
      run: melos exec -- dart analyze .

    dev:frontend:
      run: |
        open -a Simulator
        melos exec --flutter --scope=frontend -- flutter run --debug
    dev:backend:
      
