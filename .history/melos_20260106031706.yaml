name: todo_list_app

packages:
  - packages/*

melos:
  scripts:
    analyze:
      run: melos exec -- dart analyze .
    dev:frontend:
      run: |
        open -a Simulator
        melos exec --flutter --scope=frontend -- flutter run --debug