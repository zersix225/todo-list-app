name: todo_list_app

packages:
  - packages/*

workspace:
  - packages/frontend
  - packages/backend
  - packages/shared

melos:
  scripts:
    analyze:
      run: melos exec -- dart analyze .
    dev:frontend:
      run: melos exec --flutter --scope=frontend -- flutter run