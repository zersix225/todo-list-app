name: todo_list_app

packages:
  - packages/*

melos:
  scripts:
    analyze:
      run: melos exec --dart analyze .
    dev:frontend:
      run: melos exec --flutter --scope=packages/frontend -- flutter run